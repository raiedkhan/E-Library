import { MyHighlighterDirective } from './my-highlighter.directive';

xdescribe('MyHighlighterDirective', () => {
  it('should create an instance', () => {
    // const directive = new MyHighlighterDirective();
    // expect(directive).toBeTruthy();
  });
});
